{
  "name": "property-widget",
  "version": "1.0.0",
  "description": "React UI component for Property MCP Server",
  "main": "dist/component.js",
  "scripts": {
    "build": "esbuild src/index.tsx --bundle --format=esm --outfile=dist/component.js --minify --sourcemap",
    "dev": "esbuild src/index.tsx --bundle --format=esm --outfile=dist/component.js --sourcemap --watch",
    "type-check": "tsc --noEmit"
  },
  "keywords": ["mcp", "chatgpt", "property", "widget"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.3.0"
  }
}
